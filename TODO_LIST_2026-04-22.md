# SystemNix Improvement TODO List

**Created:** 2026-04-22
**Status:** In Progress

---

## Critical (Security & Correctness)

- [x] 1. Move Authelia secrets to sops-nix (`authelia.nix:19,211` — user password hash now in sops; client_secret kept as one-way hash equivalent to /etc/shadow)
- [x] 2. Fix `NODE_TLS_REJECT_UNAUTHORIZED=0` in Immich (`immich.nix:23` — replaced with `NODE_EXTRA_CA_CERTS` using system PKI certificates)
- [x] 3. Pin Docker image tags — `voice-agents.nix:106` (`:main`→`:latest`), `twenty.nix:2` (`:latest`→`0.16.2`)
- [x] 4. Pin `crush-config` flake input — `flake.nix:105` tracks `ref=master` but `flake.lock` already pins exact rev; standard flake behavior, no change needed

## High (Reliability)

- [x] 5. Add systemd hardening to all services (added PrivateTmp, NoNewPrivileges, ProtectClock, ProtectHostname, RestrictNamespaces, LockPersonality to 12/14 services; cadvisor and caddy get NoNewPrivileges=false where needed)
- [x] 6. Add `WatchdogSec=` to long-running services (added WatchdogSec=30 to authelia, homepage, signoz, signoz-collector, cadvisor, immich-server, immich-ml, photomap, caddy, taskchampion, minecraft)
- [x] 7. Fix service dependency graph (caddy→authelia, gitea sync→token, gitea-ensure-repos→token, signoz collector→clickhouse, photomap removed spurious postgresql dep)
- [x] 8. Fix `whisper-asr` service type (`voice-agents.nix:118` — changed `Type=oneshot` to `Type=forking` so systemd can track container crashes)
- [x] 9. Add `ExecStartPost` readiness checks (authelia: /api/health, signoz: /api/v1/version, homepage: /)
- [ ] 10. Fix secrets-on-disk pattern (hermes, twenty, gitea write decrypted secrets to plain-text .env)

## Medium (Nix Idioms & DRY)

- [ ] 11. Eliminate overlay/perSystem duplication in flake.nix (5 packages defined twice)
- [ ] 12. Normalize source filtering (dnsblockd uses `builtins.filterSource`, others use `lib.cleanSourceWith`)
- [ ] 13. Apply `goOverlay` consistently on Darwin (currently missing from `flake.nix:408-411`)
- [ ] 14. Extract hardcoded IPs to module options (`192.168.1.150` in 8+ files)
- [ ] 15. Extract hardcoded usernames to module options (`"lars"` in 15+ files)
- [ ] 16. Add missing option descriptions (signoz: 9 missing, monitor365: 3 missing)
- [ ] 17. Remove `monitor365-src` local path input (`flake.nix:116` — `path:/home/lars/projects/Monitor365`)
- [ ] 18. Replace imperative installs with Nix packages (wire, jscpd, aw-watcher-utilization script, brew casks)

## Medium (Testing & Validation)

- [ ] 19. Add NixOS VM tests (zero `nixosTests` exist)
- [ ] 20. Add `passthru.tests` to custom packages
- [ ] 21. Replace shell scripts with Nix apps/derivations (validate-deployment, nixos-diagnostic, ai-integration-test, test-home-manager, test-shell-aliases)

## Low (Code Quality & Cleanup)

- [ ] 22. Deduplicate Home Manager boilerplate (darwin + nixos identical 4-setting block)
- [ ] 23. Add `meta.description` to flake apps (deploy, validate, dns-diagnostics)
- [ ] 24. Clean up archived scripts (`scripts/archive/` — 10 stale scripts)
- [ ] 25. Fix gitea temp file leak (`/tmp/gitea-repos-$$.txt` without trap cleanup)
- [ ] 26. Fix gitea hardcoded UID (`uid: "1"` in 3 places)
- [ ] 27. Add monitor365 encryption (`encryption = false` at `monitor365.nix:91`)
- [ ] 28. Fix inconsistent sops secret paths in gitea-repos (direct `/run/secrets/` vs sops template)
- [ ] 29. Delete orphan packages (`notification-tone.nix`, `superfile.nix`)
- [ ] 30. Replace `bunx jscpd` devShell alias with proper Nix package
