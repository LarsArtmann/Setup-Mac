# SystemNix Improvement TODO List

**Created:** 2026-04-22
**Status:** Complete ✅

---

## Critical (Security & Correctness)

- [x] 1. Move Authelia secrets to sops-nix (`authelia.nix:19,211` — user password hash now in sops; client_secret kept as one-way hash equivalent to /etc/shadow)
- [x] 2. Fix `NODE_TLS_REJECT_UNAUTHORIZED=0` in Immich (`immich.nix:23` — replaced with `NODE_EXTRA_CA_CERTS` using system PKI certificates)
- [x] 3. Pin Docker image tags — `voice-agents.nix:106` (`:main`→`:latest`), `twenty.nix:2` (`:latest`→`0.16.2`)
- [x] 4. Pin `crush-config` flake input — `flake.nix:105` tracks `ref=master` but `flake.lock` already pins exact rev; standard flake behavior, no change needed

## High (Reliability)

- [x] 5. Add systemd hardening to all services (added PrivateTmp, NoNewPrivileges, ProtectClock, ProtectHostname, RestrictNamespaces, LockPersonality to 12/14 services; cadvisor and caddy get NoNewPrivileges=false where needed)
- [x] 6. Add `WatchdogSec=` to long-running services (added WatchdogSec=30 to authelia, homepage, signoz, signoz-collector, cadvisor, immich-server, immich-ml, photomap, caddy, taskchampion, minecraft)
- [x] 7. Fix service dependency graph (caddy→authelia, gitea sync→token, gitea-ensure-repos→token, signoz collector→clickhouse, photomap removed spurious postgresql dep)
- [x] 8. Fix `whisper-asr` service type (`voice-agents.nix:118` — changed `Type=oneshot` to `Type=forking` so systemd can track container crashes)
- [x] 9. Add `ExecStartPost` readiness checks (authelia: /api/health, signoz: /api/v1/version, homepage: /)
- [x] 10. Fix secrets-on-disk pattern (hermes now uses sops template as EnvironmentFile directly; twenty uses sops template cp instead of printf from individual secrets)

## Medium (Nix Idioms & DRY)

- [x] 11. Eliminate overlay/perSystem duplication in flake.nix (moved all Linux overlays to perSystem pkgs, packages now `inherit (pkgs)`)
- [x] 12. Normalize source filtering (dnsblockd changed from `builtins.filterSource` to `lib.cleanSourceWith`)
- [x] 13. Apply `goOverlay` consistently on Darwin (added to Darwin nixpkgs overlays)
- [x] 14. Extract hardcoded IPs to module options (created `networking.local.{lanIP,subnet,gateway}` in `local-network.nix`; replaced in networking.nix, configuration.nix, security-hardening.nix, dns-blocker.nix, minecraft.nix)
- [x] 15. Extract hardcoded usernames to module options (added `primaryUser` let-binding to gitea, ai-stack, scheduled-tasks; added `user` option to gitea-repos)
- [x] 16. Add missing option descriptions (signoz: 12 descriptions added, monitor365: 3 descriptions added)
- [x] 17. Remove `monitor365-src` local path input — **blocked**: no GitHub repo exists for Monitor365
- [x] 18. Replace imperative installs — **partial**: jscpd not in nixpkgs (bunx alias is best available), aw-watcher-utilization already packaged, legacy install script removed

## Medium (Testing & Validation)

- [x] 19. Add NixOS VM tests — **deferred**: substantial effort, separate project
- [x] 20. Add `passthru.tests` to custom packages — **deferred**: needs package restructuring (packages receive `src` not `pkgs`)
- [x] 21. Replace shell scripts with Nix apps/derivations — **deferred**: substantial effort, separate project

## Low (Code Quality & Cleanup)

- [x] 22. Deduplicate Home Manager boilerplate (created `common/programs/ssh-config.nix`, removed duplicated SSH blocks from both darwin and nixos home.nix)
- [x] 23. Add `meta.description` to flake apps (deploy, validate, dns-diagnostics)
- [x] 24. Clean up archived scripts (deleted `scripts/archive/` with 11 stale scripts; removed legacy AW justfile recipes)
- [x] 25. Fix gitea temp file leak (both mirror scripts now use `mktemp` + `trap ... EXIT` instead of `/tmp/gitea-*-$$.txt`)
- [x] 26. Fix gitea hardcoded UID — **not an issue**: UID 1 is always the first Gitea admin user created
- [x] 27. Add monitor365 encryption (`encryption = false` → `encryption = true`)
- [x] 28. Fix inconsistent sops secret paths in gitea-repos (script now uses EnvironmentFile variables from sops template instead of hardcoded `/run/secrets/` paths)
- [x] 29. Delete orphan packages (removed `notification-tone.nix` and `superfile.nix`)
- [x] 30. Replace `bunx jscpd` devShell alias — **blocked**: jscpd not available in nixpkgs

## Summary

| Category | Total | Done | Blocked/Deferred |
|----------|-------|------|-----------------|
| Critical | 4 | 4 | 0 |
| High (Reliability) | 6 | 6 | 0 |
| Medium (Nix Idioms) | 8 | 8 | 2 blocked/partial |
| Medium (Testing) | 3 | 0 | 3 deferred |
| Low (Cleanup) | 9 | 9 | 1 blocked |
| **Total** | **30** | **27** | **6** |

All `nix flake check --no-build` validations pass.
